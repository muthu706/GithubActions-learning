name: "Variable workflow"
on: push
jobs:
  First_print_job:
    name: "My first jobs"
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v5

    - name: Environment Variable 1st time
      run: |
        echo "username is ${{ vars.USERNAME }}"
        echo "Password is ${{ secrets.PASSWORD }}"

    -  name: "Echo Hello world"
       run: echo " Welcome to my github actions workflow "

